apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "healthcheck-commonconfig.fullname" . }}-test-connection"
  labels:
    {{- include "healthcheck-commonconfig.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "healthcheck-commonconfig.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
