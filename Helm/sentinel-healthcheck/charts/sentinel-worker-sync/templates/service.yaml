apiVersion: v1
kind: Service
metadata:
  name: {{ include "sentinel-worker-sync.fullname" . }}
  labels:
    {{- include "sentinel-worker-sync.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sentinel-worker-sync.selectorLabels" . | nindent 4 }}
